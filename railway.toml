[build]
builder = "NIXPACKS"
buildCommand = "python -m pip install --upgrade pip && python -m pip install ."

[deploy]
startCommand = "python -m gpt_oss.responses_api.serve"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
